import { useId } from 'react'

export function Logo(props) {
  return (
    <h1 className="text-clip bg-gradient-to-r from-yellow-200 to-yellow-400 bg-clip-text text-5xl font-semibold text-transparent">{`{noticias.dev}`}</h1>
  )
}
